<Window x:Class="Social_Sentry.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Social_Sentry"
        xmlns:vm="clr-namespace:Social_Sentry.ViewModels"
        xmlns:views="clr-namespace:Social_Sentry.Views"
        mc:Ignorable="d"
        Title="Social Sentry" Height="700" Width="1000"
        Background="{StaticResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <!-- Sidebar -->
            <ColumnDefinition Width="*"/>   <!-- Content -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource CardBackgroundBrush}" Padding="16">
            <StackPanel>
                <!-- Logo Area -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,32">
                    <!-- App Logo -->
                    <Border Width="36" Height="36" Margin="0,0,12,0" CornerRadius="8" Background="#1A00E676" Padding="4">
                        <Image Source="Images/AppLogo.png" Stretch="Uniform"/>
                    </Border>
                    
                    <TextBlock Text="Social Sentry" 
                               Foreground="{StaticResource PrimaryTextBrush}" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    
                    <!-- Enhanced Toggle Button -->
                    <ToggleButton IsChecked="{Binding IsTrackingEnabled}" 
                                  Style="{StaticResource ModernToggleStyle}"
                                  VerticalAlignment="Center"
                                  ToolTip="Toggle Tracking On/Off"
                                  Cursor="Hand"/>
                </StackPanel>

                <!-- Menu Items -->
                <Button Content="Dashboard" Command="{Binding NavigateCommand}" CommandParameter="Dashboard" Style="{StaticResource ActiveSidebarButtonStyle}" Margin="0,0,0,8"/>
                <Button Content="Categories" Command="{Binding NavigateCommand}" CommandParameter="Categories" Style="{StaticResource SidebarButtonStyle}" Margin="0,0,0,8"/>
                <Button Content="Usage Limits" Command="{Binding NavigateCommand}" CommandParameter="Limit" Style="{StaticResource SidebarButtonStyle}" Margin="0,0,0,8"/>
                <Button Content="Raw Data" Command="{Binding NavigateCommand}" CommandParameter="Raw" Style="{StaticResource SidebarButtonStyle}" Margin="0,0,0,8"/>
                <Button Content="All Features" Command="{Binding NavigateCommand}" CommandParameter="AllFeatures" Style="{StaticResource SidebarButtonStyle}" Margin="0,0,0,8"/>

                <Separator Background="#333" Margin="0,16"/>

                <!-- Bottom Items -->
                <Button Content="Settings" Command="{Binding NavigateCommand}" CommandParameter="Settings" Style="{StaticResource SidebarButtonStyle}" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                <Button Content="Get Help" Style="{StaticResource SidebarButtonStyle}" VerticalAlignment="Bottom"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Border Grid.Column="1" Background="{StaticResource WindowBackgroundBrush}">
             <!-- We use a ContentControl bound to the CurrentView property -->
             <ContentControl Content="{Binding CurrentView}">
                 <ContentControl.Resources>
                     <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                         <views:DashboardView/>
                     </DataTemplate>
                     <DataTemplate DataType="{x:Type vm:RawDataViewModel}">
                         <views:RawDataView/>
                     </DataTemplate>
                     <DataTemplate DataType="{x:Type vm:CategoryViewModel}">
                         <views:CategoryView/>
                     </DataTemplate>
                     <DataTemplate DataType="{x:Type vm:LimitViewModel}">
                         <views:LimitView/>
                     </DataTemplate>
                     <DataTemplate DataType="{x:Type vm:UserSettingsViewModel}">
                         <views:UserSettingsView/>
                     </DataTemplate>
                     <DataTemplate DataType="{x:Type vm:AllFeaturesViewModel}">
                         <views:AllFeaturesView/>
                     </DataTemplate>
                 </ContentControl.Resources>
             </ContentControl>
        </Border>

    </Grid>
</Window>
